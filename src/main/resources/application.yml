spring:
  application:
    name: inventario-backend

  datasource:
    url: jdbc:mysql://localhost:3306/gordillos_inventario_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
    username: admin_Gordillos
    password: gordillos2026!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080